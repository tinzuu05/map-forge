# Contributing

Thanks for helping improve Map Forge Studio.

## Development

```bash
npm install
npm run dev
```

## Guidelines

- Keep UI text in `src/i18n.ts` so the Chinese and English interfaces stay complete.
- Keep reusable logic outside React components when possible.
- Avoid storing uploaded user images outside the browser runtime.
- Run `npm run build` before submitting changes.
