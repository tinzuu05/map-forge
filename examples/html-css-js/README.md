# Map Forge HTML/CSS/JS Example

A plain browser example for using exported `mapProject` data with Three.js and hover popups.

Run:

```bash
python3 -m http.server 5173
```

Then open `http://localhost:5173`.
